# Copy to `.env` (gitignored) for local development.
#
# The `ContactFunctionUrl` output from the CDK stack — see infra/README.md.
# Astro's dev server proxies /api/contact here so the enquiry form works
# locally against the real Lambda. Production goes through CloudFront instead
# and needs nothing set.
CONTACT_FN_URL=
